name: mqtt_iot_app
description: A Flutter application for MQTT-based IoT system with ESP32
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mqtt_client: ^10.0.0
  sqflite: ^2.3.0
  path: ^1.8.0
  provider: ^6.0.0
  fl_chart: ^0.68.0
  intl: ^0.17.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true